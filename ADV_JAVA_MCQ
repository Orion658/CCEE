Here are the sorted questions and answers:

1. If you want to delete user details using JDBC API, which of the following is correct? 
   - Answer: D. PreparedStatement with executeUpdate()

2. How to deploy a web application listener within a web application? 
   - Answer: D. A or C

3. How many ServletConfig & ServletContext objects will be created by the web container? 
   - Answer: B. One per servlet & one per web application

4. Consider the following 2 servlets.
   ```
   @WebServlet(value="/s1", loadOnStartup=1) 
   public class FirstServlet extends HttpServlet {...} 

   @WebServlet(value="/s2") 
   public class SecondServlet extends HttpServlet {...}
   ```
   Which of the following is true? 
   - Answer: C. Web container invokes the init method of the first servlet at web app startup.

5. Which of the following attributes are inherently thread-unsafe? 
   - Answer: D. Application scoped attributes

6. What will be the output if the client sends the following request to test1.jsp? 
   ```
   http://host:port/test_web/test1.jsp?pid=101&type=electronic&name=tv
   ```
   - Answer: D. Name: tv Type:

7. To execute a stored procedure or a stored function in JDBC, what will you use? 
   - Answer: C. CallableStatement with execute

8. Default type of ResultSet is 
   - Answer: D. scrollable & read only

9. While using request chaining scenario in page navigation, what should be the minimum scope of server-side attributes?  
   - Answer: B. request scope

10. Which of the following is not accessible via EL syntax? 
    - Answer: D. session

11. Which of the following is the default behavior of Spring Security? 
    - Answer: D. All of the above

12. If you want to achieve complete DB normalization as per the Object world, which JPA inheritance strategy will you choose?  
    - Answer: C. Joined Table inheritance

13. If you want to skip a field or a property during serialization but add it during deserialization in a RESTful web service, what will you use? 
    - Answer: A. @JsonProperty(access=ACCESS.WRITE_ONLY)

14. In Hibernate, many-to-many association, which of the following is true? 
    - Answer: D. The default fetch type of this type of association is lazy.

15. Which of the following is true regarding the Spring container? 
    - Answer: B. In a standalone application, you can't launch a Spring container

16. Spring supports   
    - Answer: D. All of the above

17. In Front Controller based MVC, what is the role of the front controller? 
    - Answer: C. Centralized dispatcher

18. What will happen in the following Hibernate code, if the user id is annotated with @Id & @GeneratedValue?  
    - Answer: B. Throws PersistentObjectException

19. In a bidirectional one-to-many association, what will happen if the mappedBy attribute is not specified? 
    - Answer: C. Hibernate will create an additional table, having PKs of both the tables.

20. What will happen for the following code, when you bootstrap Hibernate? 
    - Answer: C. Table will be created with id, code & stockDesc columns

21. In a many-to-many relationship between Book & Author, what will happen if you access System.out.println(a1.getBooks()) after executing a JPQL query? 
    - Answer: B. Hibernate throws LazyInitializationException

22. Which of the following is true? 
    - Answer: C. L1 cache is implicitly enabled in Hibernate.

23. What can be said regarding the saveOrUpdate method of Hibernate? 
    - Answer: C. A & B

24. What will happen in the following Hibernate code sample if the account a with id=100 exists in DB, with balance=5000? 
    - Answer: B. Balance will be 7000

25. Which default fetching policy does JPA use for one-to-one & one-to-many? 
    - Answer: A. Eager & Lazy

26. In Spring Hibernate based web application, what can you say regarding Session's load method? 
    - Answer: A. Returns a proxy by default

27. In a many-to-many mapping, how will you specify the name of the association table, containing a composite primary key? 
    - Answer: C. @JoinTable

28. In a Spring application class, which of the following is not a part of @SpringBootApplication? 
    - Answer: C. @EnableWebMvc

29. How will you create a composite primary key class in JPA or Hibernate? 
    - Answer: A. @Embeddable

30. Which of the following is true regarding servlet filters? 
    - Answer: D. Typical use case of filters is to write cross-cutting concern.

31. What will you propose to avoid SQL injection attack? 
    - Answer: A. Use PreparedStatement API to set IN parameters.

32. Which of the following is true regarding Spring Security? 
    - Answer: D. If a hacker tampers with JWT, the Restful web service will detect an invalid JWT signature.
